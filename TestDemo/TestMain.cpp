#include <cstdio>
#include <math.h>
#include <time.h>

int main(){
	int buf[100] ;
	//FILE *fin, *fout;
	//fin = fopen("data.txt", "rb");
	//fout = fopen("data.out", "wb");
	//fgets(buf, 5, fin);
	//printf("%s", buf);
	printf("%d", sizeof(buf));
}